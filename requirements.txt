# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data validation
pydantic>=2.5.0

# Numerical computation
numpy>=1.24.0

# WebSocket support (included with fastapi/uvicorn, but explicit)
websockets>=11.0

# GPU acceleration (bundles CUDA runtime - no system CUDA needed)
cupy-cuda12x[ctk]

# Topology optimization backend (dev branch)
run_optimization_api @ git+https://github.com/gruedisueli/PyTopo3D_Backend.git@dev
